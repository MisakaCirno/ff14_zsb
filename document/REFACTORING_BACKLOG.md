# 重构追加任务清单

本清单只收录执行主计划时发现、但不应打断当前编号的工作。任务按目标阶段统一处理；除非成为数据安全或生产阻断问题，不在发现时继续向下拆分。

| ID | 目标阶段 | 优先级 | 状态 | 问题 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| B15-01 | R15 | P1 | 已完成（2026-07-15） | 审核拒绝或举报下架目前只依赖发布状态，作者再次编辑可重置状态；需要独立、持久化的审核限制语义。 | 增加明确的限制状态与原因、生产数据迁移和预检；作者编辑不得绕过下架，管理员解除限制有审计记录。 |
| B15-02 | R15 | P1 | 已完成（2026-07-15） | 详情预览 URL 直接拼接战术板代码；分享图可能在剧透或不适内容揭示前生成，并且长标题可能溢出画布。 | 预览参数安全编码；内容警告状态贯穿预览和分享图；标题按画布宽度截断或换行并有回归测试。 |
| B15-03 | R15 | P2 | 已完成（2026-07-15） | 复制逻辑和计数更新分散；剪贴板失败提示会把完整战术板代码写进全局通知。 | 统一复制反馈和计数入口；失败时提供可访问的手动复制方式，不在通知中回显完整代码。 |
| B15-04 | R15 | P2 | 已完成（2026-07-15） | 点赞和收藏采用“先查询再切换”，并发请求可能得到与用户意图相反的结果。 | 改为显式目标状态或事务化操作；并发与重复请求测试证明最终状态稳定。 |
| B15-05 | R15 | P2 | 已完成（2026-07-16） | 点赞和收藏按钮目前完全依赖 HTMX；脚本未加载或执行失败时，登录用户无法提交操作。 | 使用携带显式目标状态和安全站内返回地址的普通 POST 表单作为基础能力；HTMX 继续渐进增强局部更新，关闭 JavaScript 时仍能完成操作且不破坏现有 JSON／HTMX 契约。 |
| B16-01 | R16 | P1 | 已完成（2026-07-16） | Django Admin 的分享批量公开/私有操作仍更新已删除的 `is_public` 字段。 | 改用 `visibility` 与审核策略，补管理员动作测试和清晰反馈。 |
| B16-02 | R16 | P2 | 已完成（2026-07-16） | 前端交互目前主要依赖源码契约与浏览器手工验收；剪贴板超时、互动请求失败反馈、模态焦点、内容揭示和 Canvas 文本适配缺少可重复的 DOM／纯函数行为测试，组合 emoji 也可能在省略时被拆开；在“我的点赞／收藏”中通过 HTMX 取消后，卡片不会即时移出当前列表。 | 建立轻量前端行为测试层，覆盖上述成功与失败路径并统一点赞收藏错误反馈；保证“我的点赞／收藏”取消后的列表状态即时一致；按字素簇截断 Canvas 文本并保留兼容回退。 |
| B16-03 | R16 | P2 | 已完成（2026-07-16） | 全局主色与弱化文字令牌在浅色背景上的部分组合仅约 3.95:1 和 4.45:1；站内信等页面的小号状态文字可能达不到 WCAG AA 普通文本对比度。 | 统一调整颜色令牌及受影响组件，自动检查正常、悬停、焦点、禁用和状态色；普通文本至少 4.5:1，大号文本和非文本界面至少 3:1，并完成桌面与移动端视觉回归。 |
| B16-04 | R16 | P2 | 已完成（2026-07-16） | “我的点赞／收藏”快速并发操作中，如果列表 `outerHTML` 刷新先于另一张卡片的请求失败，已脱离 DOM 的按钮可能无法把 HTMX 失败与结束事件冒泡到文档级监听器，导致固定失败提示或请求状态清理遗漏。 | 让单卡请求的失败反馈与清理不依赖按钮仍连接在 DOM 中，或为列表刷新与卡片请求建立明确同步边界；用“列表先替换、另一请求随后失败”的回归测试证明提示去重且状态完整清理。 |
| B16-05 | R16 | P1 | 已完成（2026-07-16） | 公告、审核和举报页面的后端工作流已收敛，但页面仍保留大型单体模板、手写表单、重复模态框和分页；审核多页状态、举报长标题／长用户名／操作区及无界页码在 320px 下会溢出，独立公告页和审核／举报日志也缺少统一的响应式页面层。 | 建立可复用的管理页面布局、状态展示、表单和操作组件，使用服务端表单与共享有界分页；完成公告、审核队列、举报提交／队列及审核／举报日志的桌面、平板和 320px 长文本／多页／空态回归，后续调整 UI 不需复制业务分支或逐项修改内联样式。 |
| B16-06 | R16 | P1 | 已完成（2026-07-16） | 动态通知自动移除时可能丢失关闭按钮焦点，错误／警告缺少充足阅读时间；键盘触发无限滚动后当前按钮会随 `outerHTML` 替换消失；分享编辑器服务端错误摘要不会自动聚焦，公告动画未尊重减少动态效果偏好，互动操作组语义也不完整；管理员审核／举报表单校验失败后只显示消息并重定向，已填写说明和对应操作上下文会丢失。 | 错误与警告可由用户控制关闭且通知计时不破坏焦点；键盘触发分页后焦点进入新内容或新哨兵；编辑器错误摘要自动聚焦；公告遵守 `prefers-reduced-motion`，互动区暴露分组语义；管理员表单校验失败时保留输入、目标操作与错误焦点，并为成功、失败和焦点恢复补行为测试。 |
| B17-01 | R17 | P2 | 已完成（2026-07-16） | 详情页的审核日志、作者合集和相关合集总数没有边界；审核／举报队列还会为每个分享预取全部日志或待处理举报并生成全部操作模态框；相关合集窗口查询仍需扫描候选条目，列表卡片的双多对多计数也会产生连接乘积；Django Admin 的“选择全部结果”批量可见性操作会物化全部主键并生成无界 `IN` 查询。 | 分页或设置有解释的上限；限制管理队列的关联数据和操作结构规模；消除高基数连接乘积；将 Admin 大批量操作改为有界分批或子查询锁定，并覆盖 `select_across`；建立查询数量、返回规模和大数据量耗时基线。 |
| B19-01 | R19 | P1 | 待处理 | 当前 v2 可移植数据集的实体字段仍从运行时模型推导；未来模型增删字段可能在未提升格式版本时改变 v2 校验摘要和导入语义。 | 显式冻结 v2 的全部实体字段投影并加入固定 v1/v2 样本回归；后续结构变化必须发布 v3，旧版本的校验、摘要与导入语义保持不变。 |

“小抄儿”继续排除在本批主站重构之外；这里只允许记录其依赖的兼容性风险，不安排实现。
